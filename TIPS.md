# CLI tips

This is for tips when using the cli

## Wrong arg tips

- `uninstall`
  - use `remove` instead

- `install`
  - use `add` instead

- `compile`
  - use `build` instead

- `execute`
  - use `run` instead
